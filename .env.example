# ── Server ───────────────────────────────────────────────
PORT=3000

# ── Admin panel (/whatsapp) ──────────────────────────────
# Required. Password to access the WhatsApp admin panel,
# QR data endpoint, and session management.
ADMIN_PASSWORD=change-me

# ── Send message API ─────────────────────────────────────
# Password for POST /whatsapp/send-message endpoint.
SEND_MESSAGE_PASSWORD=change-me

# ── WhatsApp / Baileys ───────────────────────────────────
# Directory to store Baileys auth credentials.
# On Render, point this to a persistent disk (e.g. /opt/render/project/src/auth_info).
# Default: ./auth_info
AUTH_DIR=./auth_info

# ── Database (PostgreSQL) ────────────────────────────────
# Connection string for the products database.
DATABASE_URL=postgresql://user:password@host:5432/dbname
# Set to "false" to disable SSL (e.g. local development).
# Default: SSL enabled with rejectUnauthorized=false.
DB_SSL=true

# ── Frontend (order system) ───────────────────────────────
# Public URL of the frontend where users place orders.
# The bot appends ?token=<jwt> for authentication.
FRONTEND_URL=https://pedidos.example.com
# Secret key used to sign JWT tokens (30min expiry).
JWT_SECRET=change-me-to-a-strong-secret

# ── Branding ─────────────────────────────────────────────
# Path to the company logo (PNG/JPG) for the PDF catalog.
# Default: ./assets/logo.png
LOGO_PATH=./assets/logo.png

# ── Keep-alive (Render free tier) ────────────────────────
# Only activates when NODE_ENV=production AND KEEP_ALIVE_URL is set.
# The app pings itself to prevent Render from sleeping the instance.
# KEEP_ALIVE_URL=https://your-app.onrender.com/ping
KEEP_ALIVE_INTERVAL=5

# ── Auth cleanup ─────────────────────────────────────────
# Periodic cleanup of Baileys auth_info directory.
MAX_PRE_KEYS=100
MAX_AUTH_DIR_SIZE_MB=50
AUTH_CLEANUP_INTERVAL_HOURS=72
